# Insert this to the existing deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      serviceAccountName: backend-sa # Added this line
      containers:
      - name: my-container
        image: nginx:1.25.0
# ...
